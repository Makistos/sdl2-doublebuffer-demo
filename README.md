This demontstrates how to use a memory buffer with SDL2. Buffer handling is intentionally slow.

You need SDL2 installed before compiling.

Compile with 

```
gcc -lSDL2 -lSDL2main main.c -o dbdemo
```
